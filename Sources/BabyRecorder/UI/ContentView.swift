import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 16) {
            Text("Baby Recorder")
                .font(.title2)
                .fontWeight(.semibold)
            Text("Preparing MVP...")
                .foregroundStyle(.secondary)
            Spacer()
        }
        .padding(24)
    }
}
