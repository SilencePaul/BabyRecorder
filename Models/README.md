# Transcription Models

Put local speech-to-text model files here later.

The app currently reserves this directory for `whisper.cpp` style model files named `ggml-*.bin`.
Large model files are intentionally ignored by git.

